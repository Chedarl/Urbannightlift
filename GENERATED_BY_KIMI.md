# Generated by Kimi

Original prompt:

You are Kimi, a code-generation assistant. Output ONLY a single unified diff patch (plain text, ready for git apply). Do NOT output any human language, explanation, or code fences — only the unified diff.  Context to use (these are the descriptions of the screenshots and the product intent — use them to guide code and UI changes)  App title in the screenshots: "Urban Night Lift MVP app". The screenshots show an admin-style feedback / plan UI where a reviewer lists recommended features and configuration choices in a compact card/option format. Key visible text and recommendations to implement: "Shipped in this PR (the seam only...) : a schema-guarded client, an AiCall log, and an AI panel on /admin/settings next to the maps and mail readouts." "One thing I need to own: I duplicated something. /api/settings/test-email has existed since PR #52 ... I deleted mine, pointed the mail panel at the original, and removed the older standalone button so one job has one button again." "On the Google Maps catalogue idea — worth restating plainly ... Google's terms bar us from storing their Places content ... The legitimate version is better and uses the vision you're paying for: photograph a menu board, Kimi turns it into priced items." Card content shown: "Google Maps listings can't legally fill the catalogue. How should we fill it instead? Menu photos + own websites + self-signup (Recommended)"; "How should the food browsing surface fit with the order form that already exists? browse first and form prefilled but the prefilled is done at the background by Kimi ..."; "Voice transcription needs a second provider. Which, given the card trouble you had with Google? Groq Whisper — free tier, no card (Recommended)"; "Prescriptions and voice notes are covered by promises already published on your privacy page. How should I treat them? Keep them out of Kimi entirely (Recommended)". A small floating input area in screenshots labeled "Add feedback..." and a circular "Plan" button icon beside it; admin test UI shows "Test Kimi" and immediate recommended options. High-level goals (must be implemented)  Add a schema-guarded Kimi client + AiCall logging. Add an admin AI settings panel (server endpoint + simple React admin component) visible next to maps and mail readouts on /admin/settings. Panel shows toggles, telemetry summary, "Test Kimi" dry-run box and records admin actions. Implement menu-photo → priced items pipeline (vision) as the recommended catalogue population approach (merchant self-signup & photos, not Google Places import). Remove duplicate /api/settings/test-email handler and ensure the mail panel uses the canonical endpoint. Add Landmark address resolver improvements: parse landmark+area+detail and reason against ServiceLocation/VerifiedPlace rows; log to AddressResolutionLog. Add receipt OCR + payment screenshot parsing as vision features with human-in-the-loop verification (never auto-verify money). Add a transcription provider configuration with Groq Whisper as a recommended provider but default OFF; do not auto-send voice notes to Kimi. Add translation/draft replies feature (bilingual drafts: customer reply + English staff summary). Feature flags: every Kimi-dependent capability must be behind env flags and admin toggles. Safety constraints (enforce in code)  NEVER auto-verify payments, NEVER change dispatch or pricing automatically. All actions that could affect money or dispatch must create a human-review queue entry and a CaseDetail record; no money movement or dispatch modification by LLM. Do NOT store Google Places content or implement Google Places catalogue import. Voice notes and prescriptions must be opt-in per admin and audited; default is OUT (do not send any PII/voice/prescription content to Kimi by default). All Kimi calls must use strict JSON-mode prompts and backend validation; add robust JSON extraction/validation helpers. Add resolver_version constants and record them in logs. Log every model call to an AiCallLog (request, response, duration, success, meta) and write AddressResolutionLog, ReceiptVerificationLog, MenuImportCandidate and other audit rows as appropriate. Exact deliverables (files to add/modify — implement minimal but working TypeScript + tests; add TODO comments where exact Prisma model names or wiring may differ):  src/services/kimi/client.ts Export async function callKimiJSON({model, messages, timeoutMs?, maxTokens?}): returns {json: any, rawText: string, elapsedMs: number}. Read API key from process.env.MOONSHOT_API_KEY. Throw if missing. Use OpenAI-compatible HTTP call to https://api.moonshot.ai/v1/chat/completions (or existing OpenAI SDK if repo already uses it). Always include a system message: "OUTPUT ONLY JSON that conforms to the provided schema. Do not output any other text." Use a small JSON-extractor helper to strip markdown and pull top-level JSON block(s). Export an interface so unit tests can stub callKimiJSON. src/services/kimi/aiCallLog.ts Export async function logAiCall({service, endpoint, requestBody, responseBody, durationMs, success, meta}). Use prisma.aiCallLog.create(...) — add TODO if prisma model not present. Include RESOLVER_VERSION constant. src/config/kimiFeatureFlags.ts Export booleans read from env: KIMI_ADDRESS_ENABLED, KIMI_MENU_ENABLED, KIMI_VISION_ENABLED, KIMI_TRANSLATION_ENABLED, KIMI_TRANSCRIPTION_ENABLED. Default values: KIMI_MENU_ENABLED=1, KIMI_ADDRESS_ENABLED=1, KIMI_VISION_ENABLED=0, KIMI_TRANSLATION_ENABLED=1, KIMI_TRANSCRIPTION_ENABLED=0. src/utils/kimiJson.ts Safe JSON extractor: extract the first valid JSON object/array from text, strip code fences, return parsed object or throw descriptive error; used by callKimiJSON. src/services/addressResolver/kimiResolver.ts Export resolveWithKimi(rawAddress: string, context?: {lat?:number,lng?:number}). If KIMI_ADDRESS_ENABLED !== '1' return existing fallback (call existing resolver; add TODO hook). Build a strict prompt that asks Kimi to parse into JSON: {landmark, area, detail, confidence_hint}. Call callKimiJSON; log aiCall via logAiCall. Query DB for candidates: Use prisma.$queryRaw for PostGIS distance (if lat/lng provided). Example SQL in code comment; add TODO to adapt SQL to your DB. Use fuzzy ilike/trigram for names and alt_names. Compute combinedScore = weighted mix of textSim and spatialScore and verifiedBonus. (Implement simple heuristics in TypeScript; tests will mock exact DB responses.) Behavior: combinedScore >= 0.80 => record AddressResolutionLog with final_decision 'auto' and return place id + confidence. 0.30 <= combinedScore < 0.80 => write log final_decision 'review', create ReviewQueue/CaseDetail suggestion, return null + candidates. combinedScore < 0.30 => call existing Nominatim fallback, log final_decision 'fallback' and return fallback suggestion + confidence 0.5. Always write AddressResolutionLog row via prisma (TODO adapt model names). src/services/vision/receiptParser.ts Export parseReceiptImage(imageUrlOrBase64) and compareReceipt(riderTyped, parsedTotal, parsedConfidence). If KIMI_VISION_ENABLED !== '1' return null/throw. Call callKimiJSON with image payload per Kimi vision format and strict JSON schema: {merchant (string), total_amount (number), currency (string), date (ISO), items: [{name, qty, price}], confidence: number}. compareReceipt returns {match:boolean,diff:number,pctDiff:number,action:'ok'|'flag'|'human_review'}; if flag => create ReceiptVerificationLog / queue and never auto-verify. Log Ai calls and verification rows. src/services/vision/paymentParser.ts Export parsePaymentScreenshot(imageUrlOrBase64) that extracts {provider, amount, currency, sender, reference, confidence}. Create PaymentVerificationDraft (prisma) as prefill only (no verification performed). Log entry and AiCall. src/services/menus/menuParser.ts Export parseMerchantMenuImage(imageData): returns {menuRows: [{name,price,currency,variants?,raw}], confidence}. Create MenuImportCandidate rows in DB with parsed_json and status 'pending'. Create admin endpoint to list pending imports and accept/reject. Add admin UI stub component: src/admin/MenuImportReview.tsx (minimal, reads pending candidates, shows rows, accept/reject buttons that update status). Add scripts/processMenuSubmission.ts (background stub: accept image, call parser, create candidate, notify admin via CaseDetail). src/services/order/freeformParser.ts Export parseFreeformOrder(text) => {service, items, pickupText, dropoffText, notes, confidence}. Use resolveWithKimi to prefill pickup/dropoff and flag OrderDraft.needs_user_confirmation if any confidence < 0.6. src/services/kimi/translator.ts Export draftBilingualReply(caseText, customerLang?) => {customerReply, staffSummary, confidence}. Save draft to CaseDetailDraft (prisma) editable by staff. Remove duplicate endpoint Remove duplicate file(s) that implement /api/settings/test-email if a canonical route already exists. Ensure that the mail panel (frontend admin mail panel) imports/calls only the canonical endpoint from PR #52. Update UI code to reference the canonical function/path and remove the extra button. Admin settings UI & server endpoint Add server endpoint: src/pages/api/admin/kimi-settings.ts (or match repo API routing). GET returns current flags and AiCall/AddressResolution metrics aggregated (AiCall counts, success rate, address_auto_resolve_rate last 7 days). POST updates persistent toggles (if persistent store not present, write TODO comment and store toggles in a KeyValue table via prisma). Add React component: src/admin/SettingsKimiPanel.tsx to display toggles, telemetry, and "Test Kimi" dry-run box. Toggles call the admin endpoint and each toggle action writes an AdminActionLog entry (prisma) with admin id and action. Transcription provider config Add src/services/transcription/providerConfig.ts with selection via env or DB; default 'none'. Provide stub adapter for 'groq-whisper' that returns transcription only when an explicit human requests it (no automatic ingestion). Add admin UI toggle and audit requirement. scripts/translate-dictionary.ts Build-time Node script that reads src/i18n/keys.json, batches requests to callKimiJSON for translations into target languages, writes i18n/generated/<lang>.json, and exits. Do NOT auto-commit — create files only and exit; CI should open PR. Telemetry & telemetry endpoint Add simple in-memory counters module src/services/kimi/telemetry.ts and increment counters at key points; expose aggregated counts on admin settings endpoint. Tests Add tests under tests/kimi/ for each new service. Use Jest or the repo test framework. Mock callKimiJSON and prisma: verify branches for auto/resolution/review/fallback, verify logs are written, verify that money/dispatch-sensitive paths create a queue entry and do not perform side-effects. Tests must not call real Kimi. TODOs and migration guidance Add clear TODO comments where a Prisma model or migration is required (aiCallLog, AddressResolutionLog, ReceiptVerificationLog, MenuImportCandidate, AdminActionLog, CaseDetailDraft, etc.). Provide example prisma model snippets in comments so maintainers can add migrations. Implementation notes for Kimi  Strict JSON-mode: every call must include in the system message and prompt the JSON schema expected; do not allow any free text outside the JSON object. Use the JSON extractor to recover JSON safely. Admin UI design cues from screenshots: Place the AI settings panel in /admin/settings near existing maps and mail panels. Provide recommended defaults and short one-line help text for each toggle. The "Test Kimi" dry-run box should allow typing a prompt and showing the raw assistant JSON output (admin-only). Record admin toggle actions to AdminActionLog with who toggled and timestamp. Merchant menu approach: Use merchant self-signup + menu photos. Do not attempt Google Places import. Provide a simple admin review UI for parsed menu rows before accepting them into the catalogue. Return requirements  Output ONLY a unified diff patch applying the minimal TypeScript files, admin stubs, tests and removing the duplicate /api/settings/test-email implementation. Keep changes minimal, add TODO comments where wiring/migration is required, and ensure all risky features are behind flags defaulted OFF where indicated. All code paths that could affect money/dispatch must create human review queue entries and must never auto-verify or auto-commit money or dispatch changes. Now generate the unified diff patch. No extra text.

---


