# Generated by Kimi

Original prompt:

You are Kimi, a code-generation assistant. Output ONLY a single unified diff patch (plain text, ready for git apply). Do NOT output any human language, explanation, or code fences — only the unified diff. The patch will be applied to a TypeScript repo with Prisma and Node; follow the repository layout and add or modify files under src/, scripts/, and tests/. Keep all changes minimal and focused.  Goal: implement the following safe, auditable features (each change must include code, a minimal unit test, and logging to the AddressResolutionLog table via Prisma). NEVER make any automatic money transfers, dispatch changes, or auto-verify payments — always create suggestions/queues and a human verification step. Do NOT change dispatch or pricing logic. Add feature flags (environment-driven) so each capability can be turned on/off.  Feature implementations to produce:  Landmark address resolver Add src/services/kimi/client.ts: a small Kimi HTTP client using the OpenAI-compatible SDK or curl pattern reading MOONSHOT_API_KEY from env and base URL https://api.moonshot.ai/v1. Add src/services/addressResolver/kimiResolver.ts: Export async function resolveWithKimi(rawAddress: string, context?: {lat?:number,lng?:number}): Promise<{resolvedPlaceId?: number|null, confidence:number, kimiParse:any, candidates:any[]}> Send a strict JSON-mode prompt to Kimi asking to parse the rawAddress into {landmark, area, detail} and to output JSON only. Query the DB (via Prisma) for candidate ServiceLocation/VerifiedPlace rows using parsed tokens: Use fuzzy text match for name/alt_names and a PostGIS radius filter when lat/lng provided (Prisma.raw or prisma.$queryRaw). Combine text similarity and spatial proximity into a combinedScore. Add a VerifiedPlace bonus if present. Decide: if combinedScore >= 0.80 return that place id; if 0.3 <= score < 0.8 return null but enqueue for human review and include candidates; else fallback to Nominatim and return its suggestion but mark low confidence. Always write an AddressResolutionLog entry with: raw_text, kimi_parsed_json, candidates (ids+scores), final_decision, combined_score, fallback_used, resolver_version, timestamp. Export a helper to create a human review task (e.g., create a CaseDetail or queue row), but do NOT auto-accept. Receipt OCR + verification (vision) Add src/services/vision/receiptParser.ts: Export async function parseReceiptImage(imageUrlOrBase64:string): Promise<{merchant?:string,total_amount?:number,currency?:string,date?:string,items?:any[],confidence:number}> Call Kimi with an image input + JSON-mode instruction to extract merchant, total_amount, currency, date, items array and confidence. Validate numeric amounts on backend. Add compareReceipt(riderTypedAmount:number, parsedTotal:number): {match:boolean, diff:number, pctDiff:number, action:'ok'|'flag'|'human_review'} with thresholds (flag if pctDiff>0.05 or confidence<0.6). On mismatch or low confidence, write a verification queue entry and log to AddressResolutionLog or ReceiptVerificationLog (create if needed) with image reference and parsed JSON. Payment screenshot parsing (vision) Add src/services/vision/paymentParser.ts: Export async function parsePaymentScreenshot(imageUrlOrBase64:string): Promise<{provider?:string, amount?:number, currency?:string, sender?:string, reference?:string, confidence:number}> Kimi must output strict JSON fields. Backend populates a verification draft but DOES NOT mark payment verified; it only pre-fills verification UI and logs evidence. "One box" free-form order parsing Add src/services/order/freeformParser.ts: Export async function parseFreeformOrder(text:string): Promise<{service:string, items:any[], pickup:any, dropoff:any, notes?:string, confidence:number}> Kimi returns parsed structure; the backend validates addresses via resolveWithKimi and returns a prefilled OrderDraft object. If any field confidence < 0.6, mark as needs_user_confirmation. Bilingual support replies Add src/services/kimi/translator.ts: Export async function draftBilingualReply(caseText:string, customerLang?:string): Promise<{customerReply:string, staffSummary:string, confidence:number}> Kimi should detect language and produce a reply in that language plus a short English staff summary. Log drafts and mark them editable. Merchant menu parsing Add src/services/menus/menuParser.ts: Export async function parseMerchantMenu(htmlOrUrl:string): Promise<{menuRows:{name:string, price:number,currency?:string,variants?:string[],raw?:string}[], confidence:number}> Kimi extracts structured rows. Add admin verification flow: create MenuImportCandidate rows and a script to apply after manual review. Translate dictionary keys (build-time) Add scripts/translate-dictionary.ts: A node script intended to run in CI that loads the 565 keys (assume src/i18n/keys.json or similar), sends batches to Kimi for translation into target languages (e.g., fr), outputs JSON translation files under i18n/generated/, but requires human approval in a PR before merging. Safety, feature flags, and tests  Add or modify src/config/kimiFeatureFlags.ts (or env checks) so each feature runs only when process.env.KIMI_{FEATURE}_ENABLED === '1' (e.g., KIMI_ADDRESS_ENABLED). Add basic unit tests for each service under tests/kimi/ using Jest (or the repo test framework). Tests should use fixtures and mock the Kimi client to return example JSON. Add minimal assertions that logs are written and functions return the expected structure. Do NOT add any code that automatically modifies dispatch, pricing, or verifies money transfers. All actions that change money/dispatch must create a queue or PR for human review. Implementation details and expectations for the diff  Files to add/modify: at minimum add the TypeScript files above plus tests and minimal package.json devDependencies if needed (e.g., add "openai" or use fetch/curl to avoid changing package.json if possible). If you modify package.json, include a package.json patch in the diff. Use Prisma client (import prisma from 'src/prisma' or the repo’s prisma client path) to write AddressResolutionLog and any candidate/queue rows. If the exact Prisma model names differ, add clear TODO comments near DB calls to adjust fields. Include small helper utilities (src/utils/kimiJson.ts) to safely extract JSON returned by the model (strip markdown, extract JSON blocks). Add logs (console.safe or logger.info) and ensure API keys are read from process.env.MOONSHOT_API_KEY. Do NOT log the API key or full unredacted images. Testing data & sample prompt used by Kimi  For each Kimi call, instruct it in-system to "OUTPUT ONLY JSON, strictly conforming to the schema provided. Do not include any additional text." Provide expected schema examples in the prompt. Operational notes (must be enforced in the code)  Add comments and TODOs where human integration is required. Add a resolver_version constant in each file so AddressResolutionLog can record the code version. Add telemetry hooks (simple increment counters or console.info) so you can measure success rates: increase counters on successful auto-resolve, fallback, and human-review enqueues. Now generate a unified diff patch that:  Adds the requested TypeScript service files with complete function skeletons and internal implementation (Kimi calls, JSON parsing, DB lookups with Prisma.raw where needed), tests, and feature-flag checks. Adds or updates package.json only if absolutely necessary (keep changes minimal). Uses clear TODO comments where repository-specific wiring is required (exact Prisma model names or logger import). Ensures all money/dispatch sensitive operations are gated for manual human verification and not automatic. Output ONLY the unified diff patch now. No extra text.

---


