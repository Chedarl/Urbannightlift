node_modules/
.expo/
dist/
web-build/

# Generated by `expo prebuild`. Regenerate rather than commit — the app.json
# plugin config is the source of truth for permissions and the foreground
# service, and a hand-edited android/ that drifts from it is a bug nobody finds
# until a rider's tracking silently stops.
android/
ios/

.env
*.jks
*.keystore
*.p8
*.p12
*.mobileprovision
google-services.json
GoogleService-Info.plist
