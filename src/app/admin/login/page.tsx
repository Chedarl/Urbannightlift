import { LoginForm } from "@/components/shared/LoginForm";

export const dynamic = "force-dynamic";

export default function AdminLoginPage() {
  return <LoginForm subtitle="Dispatcher & admin access" />;
}
